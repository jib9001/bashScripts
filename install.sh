#!/bin/bash
sudo chmod +x aptFix && sudo cp aptFix /bin
sudo chmod +x dpkgFix && sudo cp dpkgFix /bin
sudo chmod +x gdmenuGenerator && sudo cp gdmenuGenerator /bin
sudo chmod +x protonUpdate && sudo cp protonUpdate /bin
sudo chmod +x update && sudo cp update /bin